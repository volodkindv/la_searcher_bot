name: Echo
env:
  FILE_NAME: users_activate # UPD HERE!

on:
  push:
    paths:
      - 'src/users_activate/*'
      # - .github/workflows/test2.yml
      # 77

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Echo Ok
        run: |
          echo OK
